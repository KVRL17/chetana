import { Testimonial } from "@/types";

// Keep this empty until the centre supplies and approves genuine testimonials.
export const testimonialsData: Testimonial[] = [];
export const getVisibleTestimonials = () => testimonialsData;
